<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Cat BookStore</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == 'books' && params[:action] == 'index'%>"><%=  link_to "Home", root_path %></li>
        <li class="<%= 'active' if params[:controller] == 'pages' && params[:action] == 'about'%>"><%= link_to "About", about_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">Add new<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "New Book", new_book_path %></li>
            <li><%= link_to "New Category", new_category_path %></li>
            <li><%= link_to "New Author", new_author_path %></li>
            <li><%= link_to "New Publisher", new_publisher_path %></li>
          </ul>
        </li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">Manages<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Category", categories_path %></li>
            <li><%= link_to "Author", authors_path %></li>
            <li><%= link_to "Publisher", publishers_path %></li>
          </ul>
        </li>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>